name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - target: x86_64-linux-musl
            artifact: jfai-linux-amd64
          - target: aarch64-linux-musl
            artifact: jfai-linux-arm64
          - target: x86_64-macos-none
            artifact: jfai-macos-amd64
          - target: aarch64-macos-none
            artifact: jfai-macos-arm64
    steps:
      - uses: actions/checkout@v4
      - uses: mlugg/setup-zig@v2
        with:
          version: 0.15.0
      - run: zig build -Doptimize=ReleaseSafe -Dtarget=${{ matrix.target }}
      - run: cp zig-out/bin/jfai ${{ matrix.artifact }}
      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.artifact }}

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true
      - uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*
          generate_release_notes: true

  docker:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      packages: write
    env:
      IMAGE: ghcr.io/${{ github.repository }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: jfai-linux-amd64
          path: build-amd64
      - uses: actions/download-artifact@v4
        with:
          name: jfai-linux-arm64
          path: build-arm64

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest

      # Build and push amd64
      - run: |
          cp build-amd64/jfai-linux-amd64 jfai
          docker build -t ${{ env.IMAGE }}:amd64 .
          docker push ${{ env.IMAGE }}:amd64
          rm jfai

      # Build and push arm64 via QEMU
      - uses: docker/setup-qemu-action@v3
      - run: |
          cp build-arm64/jfai-linux-arm64 jfai
          docker build --platform linux/arm64 -t ${{ env.IMAGE }}:arm64 .
          docker push ${{ env.IMAGE }}:arm64
          rm jfai

      # Create multi-arch manifest
      - run: |
          TAGS="${{ steps.meta.outputs.tags }}"
          for TAG in $TAGS; do
            docker manifest create --amend $TAG \
              ${{ env.IMAGE }}:amd64 \
              ${{ env.IMAGE }}:arm64
            docker manifest annotate $TAG ${{ env.IMAGE }}:amd64 --os linux --arch amd64
            docker manifest annotate $TAG ${{ env.IMAGE }}:arm64 --os linux --arch arm64
            docker manifest push $TAG
          done
